let interval=null,result=null;function initJank(){let t=document.getElementById("button");t.addEventListener("click",function(){null===interval?(interval=setInterval(jank,1e3/60),t.textContent="STOP JANK"):(clearInterval(interval),interval=null,t.textContent="START JANK",result.textContent="")}),result=document.getElementById("result")}function jank(){let t=0;for(let e=0;e<1e7;e++)t+=Math.random();result.textContent=t}export default initJank;